# GrabBabyShop 使用帮助

这个界面用于填写商品和支付信息、登录账号、启动抢购，并在右侧查看运行日志。

## 1. 快速开始

1. 在左侧填写 `Email`、产品编码、目标数量和支付信息。
2. 第一次使用先点击 `登录`，在弹出的浏览器中手动完成登录。
3. 登录完成后点击 `检测登录状态`，确认当前账号可用。
4. 点击 `开始抢购` 开始监控商品并自动继续后续流程。
5. 如果只是测试流程，建议先保持 `自动支付` 为关闭。

## 2. 配置项说明

- `Email`：登录时使用的邮箱地址。
- `产品编码`：只填写类似 `b50js216` 的商品编码。
- `目标数量`：希望购买的数量。
- `检查间隔`：商品缺货时的检查频率，单位为秒。
- `启用颜色选择`：开启后会按你填写的颜色条件选择商品颜色。
- `目标颜色 (正则)`：例如 `ピンク|Pink`。
- `自动支付`：开启后会在信息填写完成后继续提交支付，请谨慎使用。

## 3. 按钮说明

- `登录`：打开浏览器，供你手动完成登录。
- `检测登录状态`：检查当前登录是否有效。
- `开始抢购`：启动自动抢购流程。
- `停止`：停止当前抢购任务。
- `清空配置`：清空当前界面中的配置内容。
- `清空日志`：清空右侧日志显示。

## 4. 日志说明

- 右侧日志会显示当前步骤、错误信息和处理结果。
- 如果长时间没有变化，先查看最后几行日志，确认当前停在哪一步。
- 遇到登录、库存或支付问题时，也会在日志中显示提示。

## 5. 常见问题

- 登录后仍提示未登录：先重新点击 `检测登录状态` 确认结果。
- 登录页面无法继续：可先检查网络，必要时更换网络环境后重试。
- 抢购一直没有继续：先看右侧日志，确认是卡在商品监控、购物车还是支付页面。
- 配置修改后不生效：确认输入内容正确，并重新开始任务。
